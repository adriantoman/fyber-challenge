<% if @offers.empty? %>
    <%= erb :no_offers %>
<% else %>
    <div class="offer">
      <div class="title">Offer Title</div>
      <div class="payout">Offer Payout</div>
      <div class="thumbnail">Offer Thumbnail</div>
    </div>
    <%= partial(:offer_element, :collection => @offers) %>
<% end %>
